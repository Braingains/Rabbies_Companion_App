import React, { useEffect, useRef } from 'react';

const MapContainer = () => {

   
      return (
        <>
        <h1>Google maps sucks</h1>
        <div id="mapid"></div>
        </>
      )
}

export default MapContainer;